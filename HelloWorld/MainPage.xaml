<Page
    x:Class="Protocol2.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Protocol2"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
	<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

        <CommandBar RequestedTheme="Dark" DefaultLabelPosition="Right">
            <CommandBar.Content>
                <InkToolbar x:Name="inkToolbar"
					TargetInkCanvas="{x:Bind inkCanvas}">
                    <InkToolbarCustomToolButton x:Name="toolButtonLasso" Click="ToolButton_Lasso" ToolTipService.ToolTip="Select Strokes">
                        <SymbolIcon Symbol="{x:Bind LassoSelect}"/>
                    </InkToolbarCustomToolButton>
                </InkToolbar>
                
            </CommandBar.Content>

            <CommandBar.SecondaryCommands>
                <AppBarButton Label="Like" />
                <AppBarButton Label="Dislike"/>
            </CommandBar.SecondaryCommands>

            <AppBarButton Icon="Save" Label="Save" Click="saveAll"/>
            <AppBarButton Icon="OpenFile" Label="Load" Click="loadAll"/>
 

            <AppBarButton Icon="Back" Label="Back" Click="backToMenu"/>
            <AppBarSeparator/>
            <AppBarButton Icon="Undo" Label="Undo" Click="Undo_Click"/>
            <AppBarButton Icon="Redo" Label="Redo" Click="Redo_Click"/>
            <AppBarSeparator/>
            <AppBarButton Icon="Comment" Label="Comments"> <!-- TODO: COMMENT COLOR SELECTION -->
                    <AppBarButton.Flyout>
                    <Flyout>
                        <StackPanel>
                            <Ellipse Margin="8,8,8,8"
                                                     UseLayoutRounding="false"
                                                     HorizontalAlignment="Stretch"
                                                     VerticalAlignment="Stretch"
                                                     Height="36"
                                                     Width="36"
                                                     Fill="Goldenrod"/>
                            <Ellipse Margin="8,8,8,8"
                                                     UseLayoutRounding="false"
                                                     HorizontalAlignment="Stretch"
                                                     VerticalAlignment="Stretch"
                                                     Height="36"
                                                     Width="36"
                                                     Fill="LightSkyBlue"/>
                            <Ellipse Margin="8,8,8,8"
                                                     UseLayoutRounding="false"
                                                     HorizontalAlignment="Stretch"
                                                     VerticalAlignment="Stretch"
                                                     Height="36"
                                                     Width="36"
                                                     Fill="Plum"/>
                            <Ellipse Margin="8,8,8,8"
                                                     UseLayoutRounding="false"
                                                     HorizontalAlignment="Stretch"
                                                     VerticalAlignment="Stretch"
                                                     Height="36"
                                                     Width="36"
                                                     Fill="PaleGreen"/>
                        </StackPanel>
                    </Flyout>
                </AppBarButton.Flyout>
            </AppBarButton>
            <AppBarToggleButton Icon="MapPin" Label="Comment Mode" Click="Make_Comment"/>
            <AppBarButton Label="Combine Strokes" Click="Combine_Strokes"/>
        </CommandBar>


        <Grid Grid.Row="2">
            <InkCanvas x:Name="inkCanvas"/>
            <Canvas x:Name="canvas"/>
            <Canvas x:Name="selectionCanvas"/>

        </Grid>


		
        
	</Grid> 
</Page>
