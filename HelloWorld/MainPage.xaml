<Page
    x:Class="Protocol2.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Protocol2"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <SplitView x:Name="splitView" IsPaneOpen="False" DisplayMode="CompactInline" CompactPaneLength="50" OpenPaneLength="250" PanePlacement="Right">
        <SplitView.Pane>
            <StackPanel Background="Gray">
                <Button x:Name="exitActionsPanel" Content="&#xE700;" FontFamily="Segoe MDL2 Assets" Width="50" Height="50" Background="Transparent" HorizontalAlignment="Right" Click="Toggle_ActionBar_Pressed"/>
                <TextBlock Text="Actions" FontSize="30" HorizontalAlignment="Center" Margin="0,30,0,30" />
                <Button x:Name="combineStrokesButton" Click="Combine_Strokes" Background="Transparent" Width="250" HorizontalContentAlignment="Stretch">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xEC26;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center"/>
                        <TextBlock Text="Group Strokes" FontSize="18" VerticalAlignment="Center" Margin="20,0,0,0"/>
                    </StackPanel>
                </Button>
                <Button x:Name="drawPathButton" Background="Transparent" Width="250" HorizontalContentAlignment="Stretch" Click="TestDrawPath">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xF128;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center"/>
                        <TextBlock Text="Draw Path" FontSize="18" VerticalAlignment="Center" Margin="20,0,0,0"/>
                    </StackPanel>
                </Button>
                <Button Click="Move_Selected_Mode" Background="Transparent" Width="250" HorizontalContentAlignment="Stretch">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE759;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center"/>
                        <TextBlock Text="Move Selected" FontSize="18" VerticalAlignment="Center" Margin="20,0,0,0"/>
                    </StackPanel>
                </Button>
                <ListView x:Name="Animationlist" CanReorderItems="True" AllowDrop="True" CanDragItems="True">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <TextBlock Text="{Binding name}" Tapped="Replay"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    
                </ListView>

            </StackPanel>
        </SplitView.Pane>
        <SplitView.Content>
            <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <CommandBar x:Name="commandBar" RequestedTheme="Dark" DefaultLabelPosition="Right">
                    <CommandBar.Content>
                        <InkToolbar x:Name="inkToolbar" TargetInkCanvas="{x:Bind inkCanvas}">
                            <InkToolbarCustomToolButton x:Name="toolButtonLasso" Content="&#xEF20;" FontFamily="Segoe MDL2 Assets" Click="ToolButton_Lasso" ToolTipService.ToolTip="Select Strokes"/>
                            <InkToolbarCustomToolButton x:Name="toolButtonComment" Content="&#xE90A;" FontFamily="Segoe MDL2 Assets" Click="ToolButton_Comment" ToolTipService.ToolTip="Comment Mode"/>
                        </InkToolbar>
                    </CommandBar.Content>

                    <AppBarButton Icon="Save" Label="Save" Click="saveAll"/>
                    <AppBarButton Icon="OpenFile" Label="Load" Click="loadAll"/>
                    <AppBarSeparator/>
                    <AppBarButton Icon="Back" Label="Back" Click="backToMenu"/>
                    <AppBarSeparator/>
                    <AppBarButton Icon="Undo" Label="Undo" Click="Undo_Click"/>
                    <AppBarButton Icon="Redo" Label="Redo" Click="Redo_Click"/>
                    <AppBarSeparator/>
                    <AppBarToggleButton x:Name="AnimationToggle" Icon="SolidStar" Label="Animate" />
                    <AppBarButton Icon="OutlineStar" Label="Run Animations" Click="Animate_Test"/>
                </CommandBar>
                
                <Grid Grid.Row="2">
                    <InkCanvas x:Name="inkCanvas"/>
                    <Canvas x:Name="canvas"/>
                    <Canvas x:Name="selectionCanvas"/>
                </Grid>
            </Grid>
        </SplitView.Content>
    </SplitView>
 
</Page>
